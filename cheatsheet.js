// = assigns an oprator
// == checks if values are equal
// === checks if values and data types are equal
// != checks if values are not equal
// !== checks if values and datat types are not equal
// + adds values
// - subtracts values
// * multiplies values
// / divides values
// ++ adds 1 to a value
// -- subtracts one from a value
// > more than
// >= more than or equal to
// < less than
// <= less than or equal to
// && and
// || or